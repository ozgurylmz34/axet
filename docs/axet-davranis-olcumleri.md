# aXet.code Davranış Ölçümleri

> **Sürüm:** aXet.code 1.3.0 (Windows) · **Tarih:** 2026-09-13 · **Yöntem:** `_lab/` altında deneme projeleri;
> her aday bağlam dosyasına rastgele işaret (`MK-…`) + hiç referans verilmeyen negatif kontrol dosyası; model
> araç kullanmadan bağlamında gördüğünü listeledi (rastgele işaret tahmin edilemez). Dosya/DB/log ile çapraz kontrol.
> Yeni sürümde bu tablo yeniden ölçülmeden güvenilmez.

| Konu | Sonuç | Kanıt |
|---|---|---|
| Otomatik yüklenen bağlam dosyaları (proje kökü) | `AGENTS.md`, `CLAUDE.md`, `CLAUDE.local.md`, `GEMINI.md`, `.cursorrules`, `.github/copilot-instructions.md` | `run` ve TUI aynı 6 dosya + context_paths |
| Yüklenmeyenler | `AXET.md`, `CRUSH.md` (tek başına da), alt dizin `AGENTS.md`, üst dizin `AGENTS.md`, config dizinindeki `AGENTS.md` | işaretler çıkmadı; negatif kontrol de çıkmadı |
| `options.context_paths` | Göreli, mutlak ve dizin (içindeki .md'ler) çalışır; proje `.axet-code.json` ve global `%USERPROFILE%\.config\axet-code\axet-code.json` | global, `XDG_CONFIG_HOME` ile lab'a yönlendirilerek ölçüldü |
| `context_paths` dosyası diskte YOK (2026-09-25) | Girdi sessizce atlanır; oturum açılır, listedeki öteki bağlam dosyası yine yüklenir | 1 `run` koşusu: eksik brief + ikinci dosya → ikinci dosyanın işareti döndü, rc=0 |
| `context_paths`'teki yönergeli blok ilk mesajdan bağımsız mı (Z105, 2026-09-25) | Evet: brief dosyası bağlamdayken ilk yanıt işaretli satırla başladı — `merhaba`, dosya listeleme emri ve `%skill` ile açılışta **3/3**; aynı dosya diskte ama `context_paths`'te değilken **0/3**. `%skill` koşusunda aktarım skill'i `view` ile okuduktan sonra geldi | `run` modu, bash/edit deny; hüküm DB'den. TUI ve aynı istemlerin 3×3 tekrarı ÖLÇÜLMEDİ |
| Modelin bugünün tarihini bilmesi (brief "BAYAT" kararı) | Sistem prompt şablonunda `Today's date: {{.Date}}` satırı var | binary string (salt-okur). Modelin tarihi brief üretim tarihiyle karşılaştırıp `— BAYAT` yazdığı canlı ÖLÇÜLMEDİ |
| `options.skills_paths` + `.axet-code/skills` + `AXET_SKILLS_DIR` | Skill keşfi çalışır; `%ad` ile çağrıda model `SKILL.md` gövdesini `view` ile okur | işaret doğru döndü |
| `.claude/skills`, `.agents/skills`, `./skills` | Taranmaz | listede yok |
| Global + proje config birlikte | **Birleşir, ezmez**: `context_paths`, `skills_paths` ve `permissions.rules` iki kaynaktan birlikte uygulanır | iki bağlam işareti + 3 skill + iki deny kuralı birlikte çalıştı |
| `.axet-code/.gitignore` negasyonu (`*` + `!skills/` `!skills/**` …) | `skills/`, `commands/`, `memory/` izlenir; DB/log/codegraph yok sayılır | geçici git reposunda `git ls-files` |
| `.axetcode-denylist` | Proje kökünde düz metin, satır başına yol, `#` yorum, `~` genişler; oturum başında okunur; dosya araçlarında kesin, bash'te best-effort | aXet'in kendi sistem prompt'u (binary) — canlı ölçülmedi |
| Yerleşik sistem prompt'u | "BE AUTONOMOUS: Don't ask questions", "BE CONCISE (<4 satır)", "FOLLOW MEMORY FILE INSTRUCTIONS" içerir → çekirdek çelişkiyi açıkça çözmeli | binary string |
| Claude'a özgü frontmatter (`allowed-tools`, `user-invocable`, `argument-hint`) | Zararsız; skill yine yüklenir | listede var |
| Geçersiz YAML frontmatter (ör. tırnaksız değerde `: `) | Skill **sessizce düşürülür**: ekranda hata yok, yalnız logda `WARN Failed to parse skill file … mapping values are not allowed in this context` | 7 skill'den biri listede çıkmadı; log uyarısı yalnız o dosya için (`doctor.py` bunu yakalar) |
| Oturum açılış özeti (çekirdek §0 → `session_brief.py`) | "merhaba, işe başlayalım" istemine model önce `python "C:/AXET/scripts/session_brief.py"`'yi kendisi çalıştırdı; ilk satır kanarya, ardından main'de commit edilmemiş değişiklik uyarısı + aktif iş + ertelenmiş tetik özeti + "hangisiyle devam" sorusu | lab projesi (git, SAP, iş listesi 1 aktif + 1 tetik, devir notu), `run` modu; log `BgJob started command=python "C:/AXET/scripts/session_brief.py"`; tek koşu |
| Skill `description` uzunluğu | **1024 karakteri aşan skill yüklenmez**: ekranda hata yok, logda `WARN Skill validation failed … description exceeds 1024 characters` | 7 SAP skill'i kuruldu, model 4'ünü listeledi; düşen 3'ünün açıklaması 1031-1129, görünenlerin 673-958 karakter; log uyarısı yalnız o 3 dosya için (`doctor.py` bunu yakalar) |
| TUI Skills Manager | Yerel + `skills_paths` skill'lerini "YOURS / PROJECT" olarak listeler; Toggle/Scope/Publish/Delete düğmeleri var | ekran görüntüsü (Toggle davranışı ölçülmedi) |
| Özel komutlar `.axet-code/commands/*.md` | TUI `ctrl+p` → **User** sekmesinde `project:<ad>` | ekran görüntüsü; `$ARG` davranışı DOĞRULANMADI |
| Özel ajanlar (`.axet-code/agents/`, `%LOCALAPPDATA%\axet-code\agents`, `~\.config\axet-code\agents`, `AXET_AGENTS_DIR`) | **Çalışmaz**: `agent list --all` göstermez, sistem prompt'unda `<available_subagents>` yok, TUI'de `&` otomatik tamamlama yok | `agent create`'in kendi yazdığı dosya bile listelenmedi |
| Hook (`hooks.PreToolUse`) | **Yok**: tetiklenmedi, config şemasında alan yok | hook'un yazacağı dosya oluşmadı |
| İzin kuralları `permissions.rules` | `{"bash": {"echo *": "deny", "*": "allow"}}` proje ve global config'te çalışır, `run` modunda bile bloklar: `denied by agent permission ruleset (agent "", rule bash:"echo *"=deny)` | komutun yazacağı dosya oluşmadı |
| `permissions.rules` dosya araçları | `edit` alanındaki deny, `write` ve `edit` araçlarını birlikte bloklar (hata: `rule edit:"<desen>"=deny`); `view` serbest kalır | korunan klasörde dosya oluşmadı/değişmedi, serbest (kontrol) klasöre yazıldı |
| `edit` deseninin eşleştiği yol | **Oturum dizini içindeki** dosya göreli yolla eşleşir (`protected/*`, `*protected*` çalışır; mutlak desen `C:/…/proje/prot/*` ÇALIŞMAZ). **Dışındaki** dosya mutlak yolla eşleşir: `C:/…/dizin/*` alt klasörleri de kapsar (`**` de çalışır), `\` yazılan yol `/`'ye çevrilir. Eşleşme **büyük/küçük harfe duyarlı**: `c:/…` ve `C:/AXET/…` yazımı kuralı atlattı | log `permission.decision` alanı `pattern`: içeride `protected/a.txt`, dışarıda `C:/axet/_lab/abs_p3/sub/n.txt`; t12a-c (içeride, 3/3 yazıldı) ↔ t11 kontrol (dışarıda, bloklandı) · t14/t15 derinlik + ters bölü · harf varyantı 2/2 yazıldı |
| `install.py` klon koruması uçtan uca | Global config'e (XDG ile scratch'e yönlendirilmiş) kurulan 24 `edit` deny, başka projede açılan oturumun `C:/axet/core/_probe.txt` yazmasını blokladı; model yolu `C:/axet` yazdı (disk adı `C:\AXET`) → harf varyantları gerekli; tekrar kurulum "değişiklik yok"; `--uninstall` config'i `{}`'ye döndürdü | `denied … rule edit:"C:/axet/core/*"=deny`, dosya oluşmadı; doctor 0 FAIL |
| SAP yazma izninin bash deny koruması | `*install.py*--sap-write*` ve `*sap-write.local*` deny kuralları, modelin `python …install.py --sap --sap-write --dry-run` ve `echo x > sap-write.local` komutlarını çalıştırmasını blokladı | log `rule_pattern":"*install.py*--sap-write*"`; dosya oluşmadı |
| Global ↔ proje izin kuralı önceliği | Birleşme **desen bazında**: projede **birebir aynı desen** farklı kararla yazılırsa proje kazanır (global `echo PRECEDENCE*`=deny, proje aynı desen=allow → komut çalıştı). Projede ilgisiz desen (`echo OTHER*`=allow) ya da daha geniş desen (`echo *`=allow) global deny'ı EZMEDİ | 4 koşu: aynı desen → dosya oluştu · proje config'siz kontrol, ilgisiz desen, geniş desen → `denied … rule bash:"echo PRECEDENCE*"=deny`, dosya oluşmadı. Sonuç: `doctor.py` projede şablon desenini ezen kuralı FAIL verir |
| Alt ajan (`agent` aracı) bağlamı | Alt ajan global `context_paths` dosyalarını (çekirdek, SAP kuralları, ekip hafızası) ve proje `AGENTS.md`'sini **GÖRMEZ**; yalnız brifingde verilen metni görür | kontrol gruplu ölçüm: alt ajan `CORE-ID: YOK`, `PROJE-ISARETI: YOK`, brifing işareti `MK-BRF-2X8C` aynen döndü; aynı oturumda ana ajan `AXET-CORE-0.2.0` ve `MK-PRJ-7Q4Z` gördü; log `agent_id:"task", kind:"delegation"` |
| Kesin yasak davranışı (run modu, baskılı istem) | "Soru sorma, direkt yap" baskısında MARA'ya append/alan eklemeyi Yasak A ile, transport açıp release etmeyi Yasak C ile reddetti; alan adı önermedi; hiç komut çalıştırmadı | 2 koşu, SAP bağlantısız lab projesi; log'da bash çağrısı yok. Kanarya satırı 2 yanıtın yalnız 1'inde basıldı |
| Proje `AGENTS.md` damgasından kural yüklenmesi | Global SAP paketi kapalıyken proje `AGENTS.md`'ye basılan kesin yasak bloğu modelin bağlamına girdi | `doctor --live`: `SAP-CORE-ID: YOK · SAP-STAMP-ID: AXET-SAP-0.2.0` |
| `axet-code run` izinleri | Kural yoksa izin SORMADAN onaylar | bash komutu soru sorulmadan çalıştı |
| `axet-code run` stdin | TTY yokken stdin bekleyip askıda kalır → `</dev/null` (PowerShell: `$null \|`) şart | 400 sn timeout ↔ `</dev/null` ile 2 sn |
| Yerel MCP (`mcp` config) | **Yok sayılır**: `Local MCP configuration is ignored. Configure connectors in Agentic.` | log uyarısı; model araçları göremedi |
| Model sabitleme | Proje `.axet-code.json` → `models.large/small` global seçimi ezer | oturum DB'sinde model alanı |
| `.axet-code/.gitignore` | Varsayılan içerik `*`; dosya zaten varsa aXet ezmez | özel içerik korundu |
| Veri dizini | `.axet-code` klasörü olmayan dizinde üst dizinlerdeki `.axet-code` veri dizini olarak kullanılır | oturumlar üst dizin DB'sine yazıldı |
| Modelin gördüğü araçlar | `agent, agentic_fetch, ask_user, bash, code_graph, download, edit, fetch, glob, grep, job_kill, job_output, ls, lsp_diagnostics, lsp_references, lsp_restart, multiedit, skill_install, skill_publish, skill_search, sourcegraph, todos, view, write` | model beyanı (web_search yok) |
| Marketplace | Skill yayını daima **private**; kurulum proje ya da global kapsamlı | sistem prompt'u + TUI |
| Marketplace `skill_install` scope=project konumu (2026-09-14) | Skill `<proje>/.axet-code/skills/<ad>/SKILL.md`'ye yazılır **ve** kullanıcı düzeyi merkezi kayda düşer: `%LOCALAPPDATA%\axet-code\skills_manifest.json` → `{"skills": {"<id>": {id, name, scope, path, updated_at, visibility, active}}}`. Düz aXet projesinde `.axet-code/.gitignore` = `*` (git'e girmez). `skill_uninstall` klasörü ve manifest kaydını siler (yalnız marketplace skill'leri). Scope=global kurulum konumu **ölçülmedi** | `_lab/y8/m2`, `humanizer` (description `\|-` blok, 470 kr); kurulum sonrası dosya + manifest, kaldırma sonrası `{"skills": {}}`; **tek koşu** (`doctor.py` skill envanteri manifest'i okur) |
| Aynı adlı skill: proje `.axet-code/skills` ↔ template `skills_paths` (2026-09-14) | Model listesinde **ikisi birden** (yol bilgisiyle; proje önce) görünür, log'da uyarı yok; çağrıda model proje kopyasını okudu. "Ezer" değil: seçim modele kalır | `_lab/y8/m3`, işaretli `recall` kopyası ↔ kontrol projesi; gövde işareti proje kopyasından döndü; **tek koşu**; global dizin ↔ template önceliği ölçülmedi (`doctor.py` aynı adı FAIL verir) |
| `permissions.rules.skill_install` `{"*": "deny"}` (proje config, 2026-09-14) | Araç modelin araç setinden **tamamen çıkar** (`skill_search`/`skill_publish`/`skill_uninstall` kalır); kurulum olmaz, manifest değişmez | `_lab/y8/m4` ↔ kontrol (M2, kuralsız → kurdu); kanıt = model beyanı + klasör/manifest yokluğu (log araç filtresini yazmıyor); **tek koşu**; `ask` kararı ve global config ölçülmedi |
| Denetim | Her prompt kurumsal audit sunucusuna gönderilir | log: `Audit logged` |
| `rg` | PATH'te yoksa grep yavaşlar (log uyarısı) | log |
